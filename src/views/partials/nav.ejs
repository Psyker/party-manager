<nav>
    <div class="nav-wrapper">
        <a href="/" class="brand-logo"><% if ( user ) { %><%= user.name %>'s <% } %>Guests List</a>
        <ul id="nav-mobile" class="right hide-on-med-and-down">
            <li><a href="/guests">My Guests</a></li>
            <% if ( isAuthenticated ) { %>
                <li><a href="/logout">Logout</a></li>
            <% } %>
            <% if ( !isAuthenticated ) { %>
                <li><a href="/login">Login</a></li>
            <% } %>
        </ul>
    </div>
</nav>